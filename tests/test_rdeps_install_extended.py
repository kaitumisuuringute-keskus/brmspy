"""
Tests for brmspy.install module (extended coverage).

Focus: Installation resilience, version handling, error recovery.

Note: these can be tested with actual R package installations
across different OSes. Real installation scenarios, version handling, and
error recovery will be exercised naturally.
"""

import pytest
