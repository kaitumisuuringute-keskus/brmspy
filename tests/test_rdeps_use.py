"""
Tests for brmspy.binaries.use module (runtime installation & activation).

Focus: Runtime validation, installation orchestration, hash verification.

Note: these can be tested with actual runtime bundles, real
archive files, and actual activation scenarios across different OSes.
"""

import pytest
